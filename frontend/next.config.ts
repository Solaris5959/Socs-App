import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "vxfcyesjtrtstfphktnz.supabase.co",
      "example.com",
    ],
  },
};

export default nextConfig;
